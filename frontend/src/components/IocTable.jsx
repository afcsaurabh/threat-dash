// Reusable IOC table with sort/filter — Phase 1 implementation
// Stub in place for Phase 0 scaffold.
export default function IocTable({ iocs = [] }) {
  return (
    <div className="card">
      <p className="text-text-muted text-sm">IOC Table — Phase 1</p>
    </div>
  )
}
