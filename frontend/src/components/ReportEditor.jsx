// In-browser report assembly + export — Phase 4 implementation
// Stub in place for Phase 0 scaffold.
export default function ReportEditor({ report }) {
  return (
    <div className="card">
      <p className="text-text-muted text-sm">Report Editor — Phase 4</p>
    </div>
  )
}
